<div class="container"><h1>My new list</h1>
    <%= form_with(model: @list, url: lists_path) do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= form.submit 'Add the list' %>
  <% end %>
  <%= link_to "Back", lists_path, class: "btn btn-ghost"  %>
</div>
