<div class="container"><h1>My new list</h1>
  <%= simple_form_for [@list] do |f| %>
    <%= f.input :name%>
    <%= f.submit class: "btn btn-ghost"%>
  <% end %>
  <%= link_to "Back", lists_path, class: "btn btn-ghost"  %>
</div>
